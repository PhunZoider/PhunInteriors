# Placeholder dev map.
#
# maps/phuninteriors is borrowed space standing in for the purpose-built map
# (see CLAUDE.md, "Known gaps" #1). It is generated by the map editor rather
# than authored here, and it is bulky binary lotpack data, so it stays out of
# the repo. Remove this rule when the real map replaces it.
Contents/mods/PhunInteriors/common/media/maps/phuninteriors/
