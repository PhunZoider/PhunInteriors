# Nothing yet.
#
# maps/phuninteriors used to be excluded here while it was borrowed space
# standing in for the purpose-built map. Cells 87,46 and 88,46 are ours now,
# and the rooms do not exist without them, so they are tracked. See
# .gitattributes -- they are binary and must not be line-ending converted.
